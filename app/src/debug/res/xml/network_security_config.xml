<?xml version="1.0" encoding="utf-8"?>
<!--
    DEBUG-only network security config.

    Allows cleartext (HTTP) traffic so the emulator and devices on the LAN
    can reach development backends without TLS:
      - 10.0.2.2     → host machine from Android emulator
      - 127.0.0.1    → localhost
      - 192.168.*.*  → typical LAN ranges
      - 10.*.*.*     → corporate VPN / internal LAN

    This file ONLY exists in src/debug — release builds use the strict
    main/res/xml/network_security_config.xml (HTTPS only).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
    </domain-config>
</network-security-config>

